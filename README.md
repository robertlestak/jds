# jds - JSON Deep Sort

Sort input JSON data by key, recursively. When `jq -S` just doesn't cut it.

## Installation

```bash
curl -SsL https://raw.githubusercontent.com/robertlestak/jds/main/install_latest.sh | bash
```